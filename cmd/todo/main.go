package main

import (
	"flag"
	"github.comm/sirusxcoder/todo-app"
)

const (
	todoFile = ".todos.json"
)

func main() {
	add := flag.Bool(name:"add", value: false, usage:"add a new todo")
}

flag.Parse()

todos  &todo.Todos{}

if err != todo.
